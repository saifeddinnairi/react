import React from "react";

const Fullname = () => {
        let firstName = "obito";
        let lastName = "uchiha"; 
       return( <h3>{firstName + " " + lastName}</h3>);
   };
   export default Fullname;